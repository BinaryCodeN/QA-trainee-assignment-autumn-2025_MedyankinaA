# Баг-репорты для API микросервиса объявлений

В процессе тестирования API сервиса объявлений были выявлены следующие несоответствия документации, некорректное поведение и отсутствие базовых серверных проверок.  

## 🐞 Баг №1: Неверная структура ответа при успешном создании объявления

**Приоритет:** High  
**Серьёзность:** Major  

### Описание
При успешном создании объявления через эндпоинт `POST /api/1/item` API возвращает ответ в нестандартной структуре, которая не соответствует REST-практикам и усложняет обработку данных на клиентской стороне.

### Шаги воспроизведения
1. Выполнить POST-запрос на `/api/1/item` с валидными данными:
```json
POST /api/1/item
{
  "sellerID": 123456,
  "name": "Телефон",
  "price": 10000,
  "statistics": {
    "likes": 10,
    "viewCount": 100,
    "contacts": 5
  }
}
```

### Фактический результат
Status Code: `200 OK`  
Response Body:

```json
{
  "status": "Сохранили объявление - 2137a9b7-c138-49e8-8f6e-04aa3a4d01ae"
}
```

### Ожидаемый результат
Status Code: `200 OK`  
Response Body:

```json
{
  "id": "2137a9b7-c138-49e8-8f6e-04aa3a4d01ae",
  "sellerId": 123456,
  "name": "Телефон",
  "price": 10000,
  "statistics": {
    "likes": 10,
    "viewCount": 100,
    "contacts": 5
  },
  "createdAt": "2024-01-20T10:30:00Z"
}
```

## 🐞 Баг №2: API принимает отрицательную цену

**Приоритет:** High  
**Серьёзность:** Critical  


### Описание
API позволяет создавать объявления с отрицательной ценой, что не соответствует базовой бизнес-логике торговой площадки.

### Шаги воспроизведения
1. Отправить POST-запрос на `/api/1/item` с отрицательным значением цены:
```json
POST /api/1/item
{
  "sellerID": 123456,
  "name": "Телефон",
  "price": -100,
  "statistics": { ... }
}
```
2. Наблюдать за ответом сервера

### Фактический результат

`200 OK` --- объявление создано.

### Ожидаемый результат

`400 Bad Request` + ошибка валидации: цена должна быть `>= 0`.
```json
{
  "error": "Цена не может быть отрицательной",
  "code": "VALIDATION_ERROR"
}
```

## 🐞 Баг №3: Нет проверки диапазона sellerID

**Приоритет:** Medium  
**Серьёзность:** Major

### Описание

API не проверяет диапазон значений для sellerID, указанный в документации (111111-999999), это может привести к нарушению целостности данных.

### Шаги воспроизведения

``` json
{
  "sellerID": 100000,
  "name": "Телефон",
  "price": 10000,
  "statistics": {
    "likes": 10,
    "viewCount": 100,
    "contacts": 5
  }
}
```

### Фактический результат
Status Code: `200 OK`  
Response Body:

```json
{
  "status": "Сохранили объявление - b03ade39-2e1e-4a9c-8b3f-79a3afff406d"
}
```

### Ожидаемый результат

`400 Bad Request`.
Response Body:

```json
{
  "error": "sellerID должен быть в диапазоне от 111111 до 999999",
  "code": "VALIDATION_ERROR"
}
```

## 🐞 Баг №4: Некорректный статус код при удалении объявления

**Приоритет:** Medium  
**Серьёзность:** Major

## Описание
При попытке удаления объявления через эндпоинт DELETE `/api/2/item/{id}` API возвращает статус код 400 вместо ожидаемого 404, что противоречит стандартной практике REST API.

## Шаги воспроизведения
- Создать объявление через POST `/api/1/item`
- Получить ID созданного объявления из ответа
- Отправить DELETE-запрос на `/api/2/item/{id}`
- Наблюдать за статус кодом ответа

### Фактический результат
Status Code: `400 Bad Request`

### Ожидаемый результат
Status Code: `404 Not Found`  
Response Body:
```json
{
  "result": "laborum",
  "status": "cillum enim eiusmod"
}
```
(согласно Postman-коллекции)


## 🐞 Баг №5: Невозможность получения созданного объявления по ID

**Приоритет:** High  
**Серьёзность:** Critical

### Описание
Созданные объявления невозможно получить по их ID через эндпоинт GET `/api/1/item/{id}` - API всегда возвращает статус 404, даже для только что созданных валидных объявлений.

### Шаги воспроизведения
1. Создать объявление через `POST /api/1/item`
2. Получить ID из ответа
3. Отправить GET-запрос на `/api/1/item/{id}`
4. Наблюдать за ответом

### Фактический результат
Status Code: `404 Not Found`  
Объявление не найдено, несмотря на успешное создание

### Ожидаемый результат
Status Code: `200 OK`  
Response Body: Данные созданного объявления


## 🐞 Баг №6: Отсутствие валидации обязательных полей

**Приоритет:** High  
**Серьёзность:** Major

### Описание
API не проводит достаточную валидацию обязательных полей, что может привести к созданию объявлений с неполными данными.

### Шаги воспроизведения
Отправить POST-запрос на /api/1/item без обязательного поля sellerID:

```json
{
  "name": "Телефон без sellerID",
  "price": 10000,
  "statistics": {
    "likes": 10,
    "viewCount": 100,
    "contacts": 5
  }
}
```
Наблюдать за ответом сервера

### Фактический результат
Status Code: 400 Bad Request

Response Body:

```json
{
  "result": {
    "messages": {
      "culpa_b92": "<string>",
      "enim_24f": "<string>", 
      "mollit_aa": "<string>"
    },
    "message": "<string>"
  },
  "status": "<string>"
}
```
### Ожидаемый результат
Status Code: 400 Bad Request

Response Body:

```json
{
  "error": "Обязательное поле 'sellerID' отсутствует",
  "code": "MISSING_REQUIRED_FIELD",
  "field": "sellerID"
}
```


## 🐞 Баг №7: Несоответствие документации

**Приоритет:** Low  
**Серьёзность:** Minor

## Описание
Фактическое поведение API значительно отличается от описанного в Postman-коллекции и техническом задании.

### Области несоответствия
- Структура ответа: Фактическая vs ожидаемая по документации
- Валидация данных: Отсутствует проверка граничных значений
- Коды ошибок: Нестандартные форматы сообщений об ошибках
- Формат данных: Отличие в структуре возвращаемых объектов
- Статус коды: Фактические статус коды не соответствуют документации

### Фактический результат
API работает не в соответствии с предоставленной документацией

### Ожидаемый результат
API полностью соответствует документации и Postman-коллекции


---

## 🎯 Итоги тестирования (корректные)

**Статус:** После первоначального тестирования было исправлено большинство критических багов.

### Исправленные баги:
#### **Баг №4 - Удаление объявления**
- **Было:** Возвращало 400 Bad Request
- **Стало:** Возвращает 200 OK (удаление работает)

#### **Баг №5 - Получение объявления по ID**  
- **Было:** Всегда возвращало 404 Not Found
- **Стало:** Возвращает 200 OK с данными объявления

#### **Статистика (связано с Багом №7)**
- **Было:** Пустые массивы []
- **Стало:** Возвращает реальную статистику `[{"contacts": 5, "likes": 10, "viewCount": 100}]`

#### **Валидация пустого названия**
- **Было:** Принимало пустое название (200 OK)
- **Стало:** Корректно возвращает 400 Bad Request

### Оставшиеся баги:
#### **Баг №1 - Нестандартная структура ответа**
- Все еще возвращает `{"status": "Сохранили объявление - UUID"}` вместо полной структуры

#### **Баг №2 - Отрицательные цены**
- Все еще принимает отрицательные значения цены

#### **Баг №3 - Диапазон sellerID**  
- Все еще принимает sellerID вне диапазона 111111-999999

#### **Баг №6 - Неинформативные ошибки**
- Сообщения об ошибках все еще в нестандартном формате

**Вывод:** API значительно улучшилось после тестирования, но некоторые проблемы остаются.

